# Required: Telegram bot token from @BotFather (https://t.me/BotFather)
# 1. Open Telegram, search for @BotFather
# 2. Send /newbot, follow instructions
# 3. Copy the token (format: 123456789:ABCdefGHI...)
TELEGRAM_BOT_TOKEN=

# Required: Your numeric Telegram user ID
# Send /start to @userinfobot (https://t.me/userinfobot) to get it
ALLOWED_USER_ID=

# Claude auth: auto-detected by web-setup from `claude login` credentials.
# Full OAuth credentials are saved to secrets/claude-credentials.json (not in Docker volumes).
# Docker entrypoint injects them from secrets/.docker-env via CLAUDE_CREDENTIALS_B64 env var.
# Run `bun run web-setup` to re-detect after running `claude login`.

# Optional: OpenAI API key for semantic search (improves memory/vault retrieval)
# Get from https://platform.openai.com/api-keys
# OPENAI_API_KEY=sk-...

# Optional: Absolute path to your Obsidian vault folder
# VAULT_PATH=/path/to/obsidian/vault

# Optional: Google OAuth for proactive check-ins (managed by web-setup)
# GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
# GOOGLE_CLIENT_SECRET=your-client-secret

# Optional: Gemini API key for voice STT and/or TTS (Stage 5)
# Get from https://aistudio.google.com/apikey
# GEMINI_API_KEY=...

# Optional: ElevenLabs TTS (Stage 5)
# Get from https://elevenlabs.io — Settings → API Keys
# ELEVENLABS_API_KEY=...
# ELEVENLABS_VOICE_ID=...

# Optional: Twilio outbound calls (Stage 5)
# TWILIO_ACCOUNT_SID=...
# TWILIO_AUTH_TOKEN=...
# TWILIO_PHONE_NUMBER=...
